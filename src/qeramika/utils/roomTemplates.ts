import type { FloorPlan } from '@/qeramika/types/floorPlan';

export const generateRectangleRoom = (width: number, height: number): Partial<FloorPlan> => {
  console.warn('Room templates not yet implemented');
  return { walls: [], doors: [], windows: [], fixtures: [] };
};

export const generateLShapeRoom = (width: number, height: number): Partial<FloorPlan> => {
  console.warn('Room templates not yet implemented');
  return { walls: [], doors: [], windows: [], fixtures: [] };
};
